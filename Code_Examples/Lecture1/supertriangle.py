from turtle import *
from math import sqrt, cos, sin, radians

base_length = 15
length_increment = 3

speed('fast')

for n in range(90):
    for n in range(3):
        fd(base_length)
        lt(122)
        base_length += length_increment

mainloop()